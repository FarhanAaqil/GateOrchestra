const schedules = [['Nightly probe evaluation', 'Probe Agent', 'Daily', 'Tomorrow, 02:00', 'Active'], ['Gate fitness refresh', 'Gate Layer', 'Weekly', 'Mon, 09:30', 'Active'], ['MAS quality review', 'MAS Orchestrator', 'Monthly', 'Sep 01, 10:00', 'Paused']]
function Schedules() { return <div className="research-view"><div className="research-heading"><p className="header-kicker">Operations</p><h2>Schedules</h2><p>Frontend-only schedule preview. No tasks are sent or persisted.</p></div><div className="section-title-row"><h3>Scheduled agent tasks</h3><button className="outline-button" type="button">+ Create Schedule</button></div><section className="research-section schedule-section"><div className="schedule-row schedule-head"><span>Task name</span><span>Agent</span><span>Frequency</span><span>Next run</span><span>Status</span></div>{schedules.map((row) => <div className="schedule-row" key={row[0]}><strong>{row[0]}</strong><span>{row[1]}</span><span>{row[2]}</span><span>{row[3]}</span><span className={`schedule-status ${row[4].toLowerCase()}`}>{row[4]}</span></div>)}</section></div>}
export default Schedules